function say() {
    let chat = document.getElementById('text').value;
    let msg = "<h2>Hello  " + chat + "!</h2>";
    // document.getElementById("para").textContent = msg;
    document.getElementById("para").innerHTML = msg;

    if (chat === "Hammad") {
       let tile= document.getElementById('form').textContent = "Hello Engineer";
        document.getElementById('form').textContent;
    }

}